{
  "name": "yt-thumb-gen",
  "version": "1.0.0",
  "description": "",
  "main": "src/handler.mjs",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node src/handler.mjs",
    "zip": "rm -rf node_modules deploy.zip && npm install --only=production && zip -r deploy.zip ./ -x '.git/*'"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.19.2",
    "googleapis": "^137.1.0",
    "puppeteer-core": "^22.5.0"
  },
  "devDependencies": {
    "@sparticuz/chromium": "^122.0.0"
  }
}
